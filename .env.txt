# ============================================================================
# SKINCARE PROJECT - ENVIRONMENT VARIABLES
# ============================================================================

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=airflow
DB_USER=airflow
DB_PASSWORD=airflow

# Optional SQLAlchemy URL (used by scraper scripts)
DATABASE_URL=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow

# Airflow Configuration
AIRFLOW_URL=http://airflow-webserver:8080
AIRFLOW_USER=admin
AIRFLOW_PASSWORD=admin

# Airflow container settings (used by docker-compose)
AIRFLOW__API__AUTH_BACKENDS=airflow.api.auth.backend.basic_auth
AIRFLOW__WEBSERVER__SECRET_KEY=skincare_secret_key_2026_secure
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
AIRFLOW__CORE__LOAD_EXAMPLES=false
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=true

# Airflow admin creation on startup
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin
AIRFLOW_ADMIN_EMAIL=admin@example.com

# Flask Configuration
FLASK_ENV=production
FLASK_SECRET_KEY=skintel_secret_key_2026

# Shared data directory inside the flask container
APP_DATA_DIR=/app/data

# Scraper Configuration
SCRAPER_SCHEDULE=0 2 * * 0  # Every Sunday at 2 AM
SCRAPER_SOURCES=source1,source2,source3

# DeepSeek (OpenAI-compatible) - Ingredient enrichment
# Put your real key here (or inject via Docker secrets / CI)
DEEPSEEK_API_KEY="sk-f86dab0c76f44d09a012e691dc6243d6"
# Optional overrides
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat